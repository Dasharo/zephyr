description: NeoRV32 SPI controller
compatible: "neorv32-spi"
include: "spi-controller.yaml"

properties:
  reg:
    required: true

  syscon:
    type: phandle
    required: true
    description: |
      phandle to syscon (NEORV32 SYSINFO) node.
